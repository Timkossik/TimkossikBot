services:
  - type: web
    name: your-service-name
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python your_main_file.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.9
